services:
  database:
    image: postgres:18-alpine

    environment:
      POSTGRES_DB: reorderkit
      POSTGRES_USER: bravemeow
      POSTGRES_PASSWORD: 1234

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: